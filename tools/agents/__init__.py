"""Individual agent implementations live here.

Each agent is a plain Python module exposing a small surface that
``tools.cli`` calls into. Modules in this package MUST NOT import
each other beyond ``_common``; they are independent leaves in the
DAG defined in AGENTS.md.
"""
